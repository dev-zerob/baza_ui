import 'package:flutter/material.dart';

class ProductListScreen extends StatelessWidget {
  ProductListScreen();

  @override
  Widget build(BuildContext context) {
    return Container(
      key: Key('ProductListScreen'),
      child: Text('productListScreen'),
    );
  }
}
